# Fase 3 — La flota (un contenedor, N cuentas) · Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Correr las 5 cuentas en paralelo (una por estrategia) dentro de UN solo proceso: FastAPI sirve el panel + la API y, al arrancar, levanta una flota de hilos (uno por cuenta). Cablear el registro de estrategias al motor por cuenta, sembrar las 5 cuentas, y colapsar el `Dockerfile`/`docker-compose` a un solo servicio.

**Architecture:** El `Engine` ya trata `strategy` como opaco (solo lo pasa al `decider`), así que la flota construye un `Engine` por cuenta con `decider=get_strategy(account.strategy)` y `strategy=account.params` (dict), sin modificar el motor. Un `Fleet` lanza un hilo daemon por cuenta habilitada; cada hilo corre `engine.run_cycle(symbol)` cada `interval_seconds`. FastAPI arranca la flota en su `lifespan` (detrás de un flag de entorno, apagado por defecto para no pegarle a la red en tests) y sirve el SPA buildeado como estáticos.

**Tech Stack:** Python 3.11, FastAPI/uvicorn, threading (stdlib), pandas, ccxt. Node 22 solo en build de la imagen.

## Global Constraints

- UN proceso, UN contenedor: uvicorn `--workers 1` (con varios workers la flota se duplicaría).
- La flota NO debe arrancar en tests: gate por env `AMERICO_RUN_FLEET` (default `"0"`; la imagen Docker la pone en `"1"`).
- Riesgo GLOBAL (de `config`) para todas las cuentas en v1; riesgo por cuenta = mejora futura. Las 5 cuentas operan `BTC/USDT` (misma plaza, distinta estrategia → comparables).
- No cambiar la firma pública del `Engine` salvo afinar anotaciones; los 127 tests existentes deben seguir verdes.
- TDD. Tests con `.venv/Scripts/python.exe -m pytest -q`. `git add` de archivos específicos (NUNCA `git add -A`).
- Las velas de tests de la flota usan un feed FALSO inyectado: **cero red en pytest**.

---

### Task 1: Guarda de datos insuficientes en el motor

**Files:**
- Modify: `bot/engine/runner.py` (guarda `len(df) < 2` en `run_cycle`; afinar anotaciones de `strategy`/`decider`)
- Test: `tests/test_engine_short_df.py`

**Interfaces:**
- Consumes: `Engine` actual.
- Produces: `run_cycle` devuelve `CycleResult(symbol, "HOLD", "datos insuficientes")` sin tocar el broker/store cuando, tras descartar la vela en formación, quedan menos de 2 velas.

- [ ] **Step 1: Test que falla**

```python
# tests/test_engine_short_df.py
from __future__ import annotations

import pandas as pd

from bot.config import RiskParams, StrategyParams
from bot.engine.runner import Engine
from bot.store.db import Store


class _ShortFeed:
    def fetch_ohlcv(self, symbol, timeframe, limit=200):
        # 2 filas -> tras drop_forming_candle queda 1 -> insuficiente.
        return pd.DataFrame({
            "timestamp": pd.to_datetime([1, 2], unit="ms", utc=True),
            "open": [100, 100], "high": [100, 100],
            "low": [100, 100], "close": [100, 100], "volume": [1, 1],
        })


class _Broker:
    def cash(self): return 10000.0


def test_run_cycle_holds_on_insufficient_data():
    store = Store(":memory:")
    eng = Engine(_ShortFeed(), _Broker(), store, StrategyParams(), RiskParams(),
                 timeframe="1m", limit=2, account="acc")
    res = eng.run_cycle("BTC/USDT")
    assert res.action == "HOLD"
    assert store.recent_fills("acc") == []  # no operó
    store.close()
```

- [ ] **Step 2: Verificar que falla**

Run: `.venv/Scripts/python.exe -m pytest tests/test_engine_short_df.py -q`
Expected: FAIL (probablemente `IndexError` al acceder `iloc[-2]` en la estrategia, o no devuelve HOLD).

- [ ] **Step 3: Agregar la guarda**

En `bot/engine/runner.py`, dentro de `run_cycle`, justo después de la línea `df = drop_forming_candle(self.feed.fetch_ohlcv(symbol, self.timeframe, self.limit))`:
```python
        if len(df) < 2:
            ts = self.clock()
            self.log(f"[{symbol}] HOLD: datos insuficientes ({len(df)} velas)")
            return CycleResult(symbol, "HOLD", "datos insuficientes")
```
(Va ANTES de `price = float(df["close"].iloc[-1])`.)

También afinar las anotaciones del constructor para reflejar que la flota pasa un dict: cambiar `strategy: StrategyParams` por `strategy: object` y `decider: Callable[[pd.DataFrame, StrategyParams], Signal] = evaluate` por `decider: Callable[[pd.DataFrame, object], Signal] = evaluate`. (Solo anotaciones; el comportamiento no cambia.)

- [ ] **Step 4: Verificar verde**

Run: `.venv/Scripts/python.exe -m pytest tests/test_engine_short_df.py tests/ -q -k "engine"` → PASS

- [ ] **Step 5: Commit**

```bash
git add bot/engine/runner.py tests/test_engine_short_df.py
git commit -m "feat(engine): HOLD ante datos insuficientes (<2 velas) + anotaciones flexibles"
```

---

### Task 2: Semilla de las 5 cuentas

**Files:**
- Create: `bot/accounts.py` (`DEFAULT_ACCOUNTS`, `seed_default_accounts`)
- Test: `tests/test_accounts_seed.py`

**Interfaces:**
- Consumes: `Store.list_accounts()`, `Store.upsert_account(...)` (Fase 1).
- Produces:
  - `DEFAULT_ACCOUNTS: list[dict]` — 5 cuentas (id, name, strategy, symbol, timeframe, interval_seconds, starting_cash, ai_enabled, enabled, params).
  - `seed_default_accounts(store) -> None`: si `store.list_accounts()` está vacío, inserta las 5; si ya hay cuentas, no hace nada (idempotente).

- [ ] **Step 1: Test que falla**

```python
# tests/test_accounts_seed.py
from __future__ import annotations

from bot.accounts import DEFAULT_ACCOUNTS, seed_default_accounts
from bot.store.db import Store


def test_seeds_five_accounts_once():
    s = Store(":memory:")
    seed_default_accounts(s)
    accs = s.list_accounts()
    assert len(accs) == 5
    assert {a["strategy"] for a in accs} == {
        "ema_rsi", "macd", "bollinger", "breakout", "price_action"
    }
    # idempotente: segunda llamada no duplica
    seed_default_accounts(s)
    assert len(s.list_accounts()) == 5
    s.close()


def test_default_accounts_have_required_fields():
    for a in DEFAULT_ACCOUNTS:
        assert {"id", "name", "strategy", "symbol", "timeframe",
                "interval_seconds", "starting_cash", "ai_enabled",
                "enabled", "params"} <= set(a)
        assert isinstance(a["params"], dict)
```

- [ ] **Step 2: Verificar que falla**

Run: `.venv/Scripts/python.exe -m pytest tests/test_accounts_seed.py -q`
Expected: FAIL (`bot.accounts` no existe).

- [ ] **Step 3: Implementar**

```python
# bot/accounts.py
from __future__ import annotations

from bot.store.db import Store

DEFAULT_ACCOUNTS: list[dict] = [
    {
        "id": "scalper", "name": "Scalper EMA/RSI", "strategy": "ema_rsi",
        "symbol": "BTC/USDT", "timeframe": "1m", "interval_seconds": 12,
        "starting_cash": 10000.0, "ai_enabled": True, "enabled": True,
        "params": {"fast": 2, "slow": 4, "rsi_period": 7,
                   "rsi_oversold": 20.0, "rsi_overbought": 85.0},
    },
    {
        "id": "momentum", "name": "Momentum MACD", "strategy": "macd",
        "symbol": "BTC/USDT", "timeframe": "5m", "interval_seconds": 30,
        "starting_cash": 10000.0, "ai_enabled": True, "enabled": True,
        "params": {"fast": 12, "slow": 26, "signal": 9},
    },
    {
        "id": "reversion", "name": "Reversión Bollinger", "strategy": "bollinger",
        "symbol": "BTC/USDT", "timeframe": "15m", "interval_seconds": 60,
        "starting_cash": 10000.0, "ai_enabled": True, "enabled": True,
        "params": {"period": 20, "num_std": 2.0},
    },
    {
        "id": "ruptura", "name": "Ruptura Donchian", "strategy": "breakout",
        "symbol": "BTC/USDT", "timeframe": "30m", "interval_seconds": 120,
        "starting_cash": 10000.0, "ai_enabled": True, "enabled": True,
        "params": {"lookback": 20},
    },
    {
        "id": "price", "name": "Price Action", "strategy": "price_action",
        "symbol": "BTC/USDT", "timeframe": "1h", "interval_seconds": 180,
        "starting_cash": 10000.0, "ai_enabled": True, "enabled": True,
        "params": {"wick_ratio": 2.0},
    },
]


def seed_default_accounts(store: Store) -> None:
    if store.list_accounts():
        return
    for a in DEFAULT_ACCOUNTS:
        store.upsert_account(
            a["id"], a["name"], a["strategy"], a["symbol"], a["timeframe"],
            a["interval_seconds"], a["starting_cash"], a["ai_enabled"],
            a["enabled"], a["params"],
        )
```

- [ ] **Step 4: Verificar verde**

Run: `.venv/Scripts/python.exe -m pytest tests/test_accounts_seed.py -q` → PASS

- [ ] **Step 5: Commit**

```bash
git add bot/accounts.py tests/test_accounts_seed.py
git commit -m "feat(accounts): semilla de las 5 cuentas (idempotente)"
```

---

### Task 3: Orquestador de la flota

**Files:**
- Create: `bot/fleet.py` (`Fleet`)
- Test: `tests/test_fleet.py`

**Interfaces:**
- Consumes: `Store`, `Config` (`exchange`, `broker`, `risk`, `ai`), `get_strategy` (Fase 2), `Engine`, `LocalPaperBroker`, `CcxtDataFeed`, `build_advisor`/`ai_affects_execution` (de `bot/cli.py`), `DataFeed`.
- Produces:
  - `Fleet(store, config, feed_factory: Callable[[], DataFeed] | None = None)`.
  - `Fleet.run_once() -> None`: corre un ciclo por cada cuenta habilitada (construye los engines la primera vez). Aísla fallos por cuenta.
  - `Fleet.start() -> None`: lanza un hilo daemon por cuenta habilitada (loop con `interval_seconds`, interrumpible).
  - `Fleet.stop(timeout: float = 5.0) -> None`: señala el corte y joinea los hilos.
  - `Fleet._build_engine(account: dict) -> Engine`: arma feed, broker (cash de `store.latest_equity(id)` o `starting_cash`; holdings de `store.get_positions(id)`), advisor (según `ai_enabled`), engine (`decider=get_strategy(strategy)`, `strategy=params`, `account=id`, `timeframe`).

- [ ] **Step 1: Test que falla**

```python
# tests/test_fleet.py
from __future__ import annotations

import pandas as pd

from bot.config import Config
from bot.fleet import Fleet
from bot.store.db import Store


def _ramp_feed():
    # Sube fuerte: gatilla el cruce de EMA del scalper (ema_rsi).
    class F:
        def fetch_ohlcv(self, symbol, timeframe, limit=200):
            closes = [10.0] * 6 + [11, 12, 13, 14, 15, 16, 17]
            n = len(closes)
            return pd.DataFrame({
                "timestamp": pd.to_datetime(range(n), unit="ms", utc=True),
                "open": closes, "high": [c + 1 for c in closes],
                "low": [c - 1 for c in closes], "close": closes, "volume": [1] * n,
            })
    return F()


def _seed_one(store):
    store.upsert_account("scalper", "Scalper", "ema_rsi", "BTC/USDT", "1m", 1,
                         10000.0, False, True, {"fast": 2, "slow": 4, "rsi_overbought": 90})


def test_run_once_records_decision_per_account():
    store = Store(":memory:")
    _seed_one(store)
    fleet = Fleet(store, Config(), feed_factory=lambda: _ramp_feed())
    fleet.run_once()
    assert len(store.recent_decisions("scalper")) == 1
    store.close()


def test_disabled_account_is_skipped():
    store = Store(":memory:")
    store.upsert_account("off", "Off", "ema_rsi", "BTC/USDT", "1m", 1,
                         10000.0, False, False, {"fast": 2, "slow": 4})
    fleet = Fleet(store, Config(), feed_factory=lambda: _ramp_feed())
    fleet.run_once()
    assert store.recent_decisions("off") == []
    store.close()


def test_start_then_stop_is_clean():
    store = Store(":memory:")
    _seed_one(store)
    fleet = Fleet(store, Config(), feed_factory=lambda: _ramp_feed())
    fleet.start()
    fleet.stop(timeout=2.0)
    assert all(not t.is_alive() for t in fleet._threads)
    store.close()
```

- [ ] **Step 2: Verificar que falla**

Run: `.venv/Scripts/python.exe -m pytest tests/test_fleet.py -q`
Expected: FAIL (`bot.fleet` no existe).

- [ ] **Step 3: Implementar `Fleet`**

```python
# bot/fleet.py
from __future__ import annotations

import threading
from typing import Callable

from bot.broker.paper import LocalPaperBroker
from bot.cli import ai_affects_execution, build_advisor
from bot.config import Config
from bot.data.feed import CcxtDataFeed, DataFeed
from bot.engine.runner import Engine
from bot.store.db import Store
from bot.strategy.registry import get_strategy


class Fleet:
    def __init__(
        self,
        store: Store,
        config: Config,
        feed_factory: Callable[[], DataFeed] | None = None,
        log: Callable[[str], None] = print,
    ) -> None:
        self.store = store
        self.config = config
        self.feed_factory = feed_factory or (lambda: CcxtDataFeed(config.exchange))
        self.log = log
        self._pairs: list[tuple[dict, Engine]] = []
        self._threads: list[threading.Thread] = []
        self._stop = threading.Event()

    def _build_broker(self, account: dict) -> LocalPaperBroker:
        bp = self.config.broker
        eq = self.store.latest_equity(account["id"])
        cash = eq[1] if eq is not None else account["starting_cash"]
        positions = self.store.get_positions(account["id"])
        holdings = {s: p.quantity for s, p in positions.items()} or None
        return LocalPaperBroker(cash, bp.fee_rate, bp.slippage, holdings=holdings)

    def _build_engine(self, account: dict) -> Engine:
        ai_on = bool(account["ai_enabled"])
        # build_advisor mira config.ai.enabled; lo respetamos por cuenta clonando el flag.
        cfg = self.config
        advisor = build_advisor(cfg) if (ai_on and cfg.ai.enabled) else None
        return Engine(
            feed=self.feed_factory(),
            broker=self._build_broker(account),
            store=self.store,
            strategy=account["params"],
            risk=cfg.risk,
            timeframe=account["timeframe"],
            limit=cfg.limit,
            decider=get_strategy(account["strategy"]),
            advisor=advisor,
            ai_affects_execution=(ai_on and cfg.broker.kind == "paper"),
            account=account["id"],
        )

    def _setup(self) -> None:
        if self._pairs:
            return
        self._pairs = [
            (a, self._build_engine(a))
            for a in self.store.list_accounts()
            if a["enabled"]
        ]

    def run_once(self) -> None:
        self._setup()
        for account, engine in self._pairs:
            try:
                engine.run_cycle(account["symbol"])
            except Exception as exc:  # noqa: BLE001 - aislar fallos por cuenta
                self.log(f"[{account['id']}] ERROR: {exc}")

    def _loop(self, account: dict, engine: Engine) -> None:
        while not self._stop.is_set():
            try:
                engine.run_cycle(account["symbol"])
            except Exception as exc:  # noqa: BLE001
                self.log(f"[{account['id']}] ERROR: {exc}")
            self._stop.wait(account["interval_seconds"])

    def start(self) -> None:
        self._setup()
        self._stop.clear()
        for account, engine in self._pairs:
            t = threading.Thread(
                target=self._loop, args=(account, engine),
                name=f"fleet-{account['id']}", daemon=True,
            )
            t.start()
            self._threads.append(t)
        self.log(f"Flota arriba: {len(self._threads)} cuentas")

    def stop(self, timeout: float = 5.0) -> None:
        self._stop.set()
        for t in self._threads:
            t.join(timeout)
        self._threads.clear()
```

Nota: `build_advisor`/`ai_affects_execution` viven en `bot/cli.py`; importarlos no genera ciclo (cli no importa fleet).

- [ ] **Step 4: Verificar verde**

Run: `.venv/Scripts/python.exe -m pytest tests/test_fleet.py -q` → PASS

- [ ] **Step 5: Commit**

```bash
git add bot/fleet.py tests/test_fleet.py
git commit -m "feat(fleet): orquestador de N cuentas (hilo por cuenta, run_once para tests)"
```

---

### Task 4: FastAPI levanta la flota + sirve el SPA

**Files:**
- Modify: `api/app.py` (lifespan con gate de entorno; servir estáticos del SPA con fallback)
- Test: `tests/test_app_static.py` + confirmar que los 127 tests siguen verdes

**Interfaces:**
- Consumes: `seed_default_accounts` (Task 2), `Fleet` (Task 3), `load_config`, `Store`.
- Produces:
  - `create_app()` registra un `lifespan` que, SOLO si `os.environ.get("AMERICO_RUN_FLEET", "0") == "1"`, siembra cuentas y arranca la flota (y la frena en shutdown).
  - Si existe el directorio `AMERICO_WEB_DIST` (default `"web_dist"`), monta `/assets` y agrega un catch-all que devuelve `index.html` para rutas que no son `/api/...` (SPA fallback). Si no existe, no agrega nada (tests sin build).

- [ ] **Step 1: Test que falla**

```python
# tests/test_app_static.py
from __future__ import annotations

import os

from fastapi.testclient import TestClient

from api.app import create_app


def test_api_still_works_without_web_dist(monkeypatch):
    # Sin build del SPA, /api sigue respondiendo y no se monta estático.
    monkeypatch.setenv("AMERICO_RUN_FLEET", "0")
    monkeypatch.setenv("AMERICO_WEB_DIST", "no_existe_dir")
    client = TestClient(create_app())
    assert client.get("/api/health").json() == {"status": "ok"}


def test_spa_fallback_when_dist_exists(tmp_path, monkeypatch):
    dist = tmp_path / "dist"
    (dist / "assets").mkdir(parents=True)
    (dist / "index.html").write_text("<!doctype html><title>app</title>", encoding="utf-8")
    monkeypatch.setenv("AMERICO_RUN_FLEET", "0")
    monkeypatch.setenv("AMERICO_WEB_DIST", str(dist))
    client = TestClient(create_app())
    # ruta de cliente -> index.html
    r = client.get("/cualquier/ruta")
    assert r.status_code == 200 and "<title>app</title>" in r.text
    # /api sigue teniendo prioridad
    assert client.get("/api/health").json() == {"status": "ok"}
```

- [ ] **Step 2: Verificar que falla**

Run: `.venv/Scripts/python.exe -m pytest tests/test_app_static.py -q`
Expected: FAIL (no hay catch-all SPA; `AMERICO_WEB_DIST` no se lee).

- [ ] **Step 3: Modificar `api/app.py`**

1. Agregar imports arriba:
```python
from contextlib import asynccontextmanager

from fastapi.responses import FileResponse
from fastapi.staticfiles import StaticFiles

from api.deps import CONFIG_PATH
from bot.accounts import seed_default_accounts
from bot.config import load_config
from bot.fleet import Fleet
```
2. Definir el lifespan ANTES de `create_app`:
```python
def _make_lifespan():
    @asynccontextmanager
    async def lifespan(app):
        fleet = None
        if os.environ.get("AMERICO_RUN_FLEET", "0") == "1":
            config = load_config(CONFIG_PATH)
            store = Store(config.db_path)
            seed_default_accounts(store)
            fleet = Fleet(store, config)
            fleet.start()
        try:
            yield
        finally:
            if fleet is not None:
                fleet.stop()
    return lifespan
```
3. En `create_app`, pasar el lifespan: `app = FastAPI(title="AMÉRICO API", version="2.0.0", lifespan=_make_lifespan())`.
4. AL FINAL de `create_app` (después de registrar TODAS las rutas `/api/...`, antes de `return app`), montar el SPA:
```python
    web_dist = os.environ.get("AMERICO_WEB_DIST", "web_dist")
    assets = os.path.join(web_dist, "assets")
    index = os.path.join(web_dist, "index.html")
    if os.path.isdir(web_dist) and os.path.isfile(index):
        if os.path.isdir(assets):
            app.mount("/assets", StaticFiles(directory=assets), name="assets")

        @app.get("/{full_path:path}")
        def spa(full_path: str):
            if full_path.startswith("api/"):
                from fastapi import HTTPException
                raise HTTPException(status_code=404)
            return FileResponse(index)
```

- [ ] **Step 4: Verificar verde (nuevo + suite completa)**

Run: `.venv/Scripts/python.exe -m pytest -q` → PASS (los 127 + los nuevos). La flota NO arranca en tests porque `AMERICO_RUN_FLEET` no está seteada (default `"0"`).

- [ ] **Step 5: Commit**

```bash
git add api/app.py tests/test_app_static.py
git commit -m "feat(api): lifespan arranca la flota (gated) y sirve el SPA con fallback"
```

---

### Task 5: Un solo contenedor (Dockerfile + compose)

**Files:**
- Modify: `Dockerfile` (multi-stage: build del SPA + Python que sirve todo y corre la flota)
- Modify: `docker-compose.yml` (un solo servicio `app`)
- Modify: `.dockerignore` (no excluir `web/`, sí su `node_modules`/`dist`)
- Modify: `config.docker.yaml` (riesgo moderado para la flota multi-timeframe)
- Delete: `web/Dockerfile`, `web/nginx.conf` (ya no hay contenedor nginx)
- Test: build local (manual, ver Step 5)

**Interfaces:** ninguna nueva de código; cambia el empaquetado.

- [ ] **Step 1: Reescribir `Dockerfile`**

```dockerfile
# --- build del panel (Vite) ---
FROM node:22-alpine AS web
WORKDIR /web
COPY web/package.json web/package-lock.json ./
RUN npm install --no-audit --no-fund
COPY web/ ./
ENV VITE_API_BASE=""
RUN npm run build

# --- app: API + panel + flota, en un solo proceso ---
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY bot ./bot
COPY api ./api
COPY config.docker.yaml ./config.docker.yaml
COPY --from=web /web/dist ./web_dist
RUN mkdir -p /data
ENV PYTHONUNBUFFERED=1 \
    AMERICO_CONFIG=config.docker.yaml \
    AMERICO_WEB_DIST=/app/web_dist \
    AMERICO_RUN_FLEET=1
CMD ["uvicorn", "api.app:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "1"]
```

- [ ] **Step 2: Reescribir `docker-compose.yml`**

```yaml
# Stack de AMÉRICO en UN solo contenedor: API + panel + flota de 5 cuentas.
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: americo:latest
    ports:
      - "${WEB_PORT:-8080}:8000"
    environment:
      PYTHONUNBUFFERED: "1"
      AMERICO_CONFIG: config.docker.yaml
      AMERICO_CORS_ORIGINS: ${AMERICO_CORS_ORIGINS:-*}
      AMERICO_RUN_FLEET: "1"
      # Opcional: Postgres gestionado de Coolify. Si está, la DB usa Postgres.
      DATABASE_URL: ${DATABASE_URL:-}
      # IA (opcional): la veta entradas por cuenta si está la key.
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OKX_API_KEY: ${OKX_API_KEY:-}
      OKX_API_SECRET: ${OKX_API_SECRET:-}
      OKX_API_PASSWORD: ${OKX_API_PASSWORD:-}
    volumes:
      - americo-data:/data
    restart: unless-stopped

volumes:
  americo-data:
```

- [ ] **Step 3: Actualizar `.dockerignore`**

Reemplazar el contenido por (la imagen ahora SÍ necesita `web/` para el build del SPA, pero no su `node_modules`/`dist`):
```
.venv/
**/__pycache__/
*.pyc
.git/
.github/
.pytest_cache/
*.sqlite
web/node_modules/
web/dist/
docs/
tests/
.env
config-private.yaml
*.md
.claude/
```

- [ ] **Step 4: Moderar el riesgo en `config.docker.yaml`**

En `config.docker.yaml`, sección `risk:`, dejar valores moderados que sirvan en todos los timeframes de la flota (el 0.18%/0.26% del scalper era para demo de 1m):
```yaml
risk:
  risk_per_trade: 0.02
  stop_loss_pct: 0.01
  take_profit_pct: 0.02
  max_exposure_pct: 0.50
  max_positions: 2
```
(El `timeframe`/`loop_interval_seconds`/`strategy` de nivel superior ya no los usa la flota —usa las cuentas—, pero dejalos como están para el CLI `python -m bot run` legado.)

- [ ] **Step 5: Borrar el contenedor web viejo y buildear**

```bash
git rm web/Dockerfile web/nginx.conf web/.dockerignore
docker compose build
```
Expected: build OK de la imagen única `americo:latest` (stage `web` corre `npm run build`; stage Python instala requirements y copia `web_dist`).

Smoke test:
```bash
WEB_PORT=8080 docker compose up -d
# esperar ~10s
curl -s http://localhost:8080/api/accounts        # 5 cuentas sembradas
curl -s http://localhost:8080/api/health           # {"status":"ok"}
curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/   # 200 (panel)
docker compose logs app --tail 20                  # "Flota arriba: 5 cuentas" + decisiones
docker compose down
```

- [ ] **Step 6: Commit**

```bash
git add Dockerfile docker-compose.yml .dockerignore config.docker.yaml
git rm web/Dockerfile web/nginx.conf web/.dockerignore
git commit -m "feat(docker): un solo contenedor (API + panel + flota); compose colapsado"
```

---

### Task 6: Test de estrella fugaz + smoke de integración de la flota

**Files:**
- Test: `tests/test_strategy_price_action_star.py`, `tests/test_fleet_integration.py`

**Interfaces:**
- Consumes: `decide_price_action` (Fase 2), `Fleet` + `seed_default_accounts`.

- [ ] **Step 1: Tests que fallan/pasan**

```python
# tests/test_strategy_price_action_star.py
from __future__ import annotations

import pandas as pd

from bot.models import Action
from bot.strategy.price_action import decide_price_action


def test_shooting_star_sells():
    # Vela previa neutra; última con mecha superior larga y cuerpo chico -> SELL.
    rows = [(10, 10.5, 9.5, 10), (10.0, 11.0, 9.95, 10.05)]
    # cuerpo 0.05, mecha sup = 11.0 - 10.05 = 0.95 (>= 2*cuerpo), mecha inf = 9.95 baja poco
    df = pd.DataFrame({
        "timestamp": pd.to_datetime(range(len(rows)), unit="ms", utc=True),
        "open": [r[0] for r in rows], "high": [r[1] for r in rows],
        "low": [r[2] for r in rows], "close": [r[3] for r in rows], "volume": [1] * len(rows),
    })
    sig = decide_price_action(df, {"wick_ratio": 2.0})
    assert sig.action is Action.SELL
```

```python
# tests/test_fleet_integration.py
from __future__ import annotations

import pandas as pd

from bot.accounts import seed_default_accounts
from bot.config import Config
from bot.fleet import Fleet
from bot.store.db import Store


def _feed():
    class F:
        def fetch_ohlcv(self, symbol, timeframe, limit=200):
            closes = [100.0 + (i % 7) for i in range(60)]
            n = len(closes)
            return pd.DataFrame({
                "timestamp": pd.to_datetime(range(n), unit="ms", utc=True),
                "open": closes, "high": [c + 1 for c in closes],
                "low": [c - 1 for c in closes], "close": closes, "volume": [1] * n,
            })
    return F()


def test_fleet_runs_all_five_accounts_once():
    store = Store(":memory:")
    seed_default_accounts(store)
    fleet = Fleet(store, Config(), feed_factory=lambda: _feed())
    fleet.run_once()
    # cada cuenta registró al menos una decisión bajo su id
    for acc_id in ("scalper", "momentum", "reversion", "ruptura", "price"):
        assert len(store.recent_decisions(acc_id)) == 1
    store.close()
```

- [ ] **Step 2: Verificar (ajustar velas si hace falta, como en Fase 2)**

Run: `.venv/Scripts/python.exe -m pytest tests/test_strategy_price_action_star.py tests/test_fleet_integration.py -q`
Expected: PASS. Si la estrella fugaz no dispara, ajustá los valores de la vela (no la lógica). El smoke de la flota solo verifica que las 5 cuentas registran decisión (la acción puede ser HOLD).

- [ ] **Step 3: Suite completa + commit**

Run: `.venv/Scripts/python.exe -m pytest -q` → PASS (todos)
```bash
git add tests/test_strategy_price_action_star.py tests/test_fleet_integration.py
git commit -m "test: estrella fugaz (price action) + smoke de integracion de la flota"
```

---

## Notas de cierre

- Tras la Fase 3, `docker compose up` levanta **un solo contenedor** que sirve el panel y la API y corre las **5 cuentas en paralelo**, cada una con su estrategia y cadencia, escribiendo a SQLite (o Postgres si hay `DATABASE_URL`). La IA veta entradas por cuenta si está `ANTHROPIC_API_KEY`.
- El panel todavía muestra UNA cuenta (la `default` / la que pidas por `?account=`); el **selector de cuenta + tablero comparativo** es la **Fase 4**.
- Riesgo global v1: si una estrategia de timeframe alto se ve muy frenada por el stop, en Fase 4 se puede mover el riesgo a `params` por cuenta.
